import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'OD Labs - Innovative Tech Solutions',
  description: 'Leading provider of Website Development, AI/ML Solutions, and Cloud Solutions',
  keywords: 'web development, AI, ML, cloud solutions, software development, Next.js, React, TypeScript',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
